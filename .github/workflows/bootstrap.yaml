name: BootStrap Argo

run-name: Bootstrap cluster ${{ inputs.cluster_name }}

on: 
  workflow_dispatch:
    inputs:
      cluster_name:
        description: 'Select the cluster to bootstrap'
        required: true
        default: 'hdti'
        type: choice
        options:
          - hdti

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  bootstrap_cluster:
    runs-on: self-hosted
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      # Dependencies
      - name: Install kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: 'latest'

      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: 'latest'

      # Secrets
      - name: Import Secrets from Vault
        uses: hashicorp/vault-action@v3
        with:
          url: http://192.168.0.188:8200
          method: approle
          roleId: ${{ secrets.ROLEID }}
          secretId: ${{ secrets.SECRETID }} 
          secrets: |
            secrets/data/k8s/kubeconfig config | KUBECONFIG_DATA

      - name: Set Kubernetes Context
        uses: azure/k8s-set-context@v4
        with:
          method: kubeconfig
          kubeconfig: ${{ env.KUBECONFIG_DATA }}

      - name: BootStrap Argo
        run: |
          # Use github.event.inputs for maximum compatibility
          SCRIPT_PATH="./clusters/${{ github.event.inputs.cluster_name }}/bootstrap.sh"
          chmod +x "$SCRIPT_PATH"
          ./"$SCRIPT_PATH"
        env:
          KUBECONFIG: ${{ env.KUBECONFIG }} # Ensure the script sees the file path